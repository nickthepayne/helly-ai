# Copy to ai/.env and fill with your keys. These are read at AI process startup.
# Values in .env do not override existing environment variables.

# Required for OpenRouter LLM
OPENROUTER_API_KEY=
OPENROUTER_MODEL=deepseek/deepseek-chat-v3-0324:free

# Optional: where to persist Chroma DB
CHROMA_PERSIST_DIR=.chroma

