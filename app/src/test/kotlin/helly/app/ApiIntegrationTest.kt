package helly.app

import org.junit.jupiter.api.Test

class ApiIntegrationTest {
    @Test
    fun `should create member add feedback and ask`() {
        // TODO: SpringBootTest + MockMvc scaffold (left intentionally minimal)
        // Steps:
        // 1) POST /v1/team-members
        // 2) POST /v1/feedback with content and person_hint
        // 3) POST /v1/ask with question and person_hint
        // Mock AiClient and EntityResolutionService
        assert(true)
    }
}

